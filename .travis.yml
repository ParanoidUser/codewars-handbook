dist: bionic
language: java

git:
  depth: false

jdk: openjdk11

install: skip

script:
  - ./gradlew -Pversion=$VERSION check $SONAR_OPTS sonarqube --parallel --no-daemon --build-cache --scan

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
  - rm -fr $HOME/.gradle/caches/*/scripts/

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
    - $HOME/.sonar/cache/